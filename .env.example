# LLM Configuration
# Provider: claude-cli (uses local Claude Code CLI + Max subscription)
#           openai | anthropic (direct API access, requires API key)
LLM_PROVIDER=claude-cli
LLM_MODEL=sonnet

# Only needed for openai/anthropic providers:
# LLM_API_URL=https://api.openai.com/v1
# LLM_API_KEY=sk-your-key-here
# LLM_MAX_TOKENS=2048
# LLM_TEMPERATURE=0.7
