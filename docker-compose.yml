services:
  # ============================================
  # Presentation (nginx — static files)
  # Backend runs on host for Claude CLI access
  # ============================================
  rex:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: rex-presentation
    restart: unless-stopped
    ports:
      - "8090:8080"
